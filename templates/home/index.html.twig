{% extends 'base.html.twig' %}

{% block title %}HomePage{% endblock %}

{% block body %}
    {% if annoucements is defined %}
    <div class="homeAnnoucements">
        {% for a in annoucements %}
            {{ include('annoucement.html.twig', {
                annoucement:a,
                link:true,
                }) }}
        {% endfor %}
    </div>
    {% endif %}
{% endblock %}
